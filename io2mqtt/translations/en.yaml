configuration:
  mqtt_io_config_path:
    name: MQTT-IO config file path
    description: Full path to the MQTT-IO YAML configuration file (must be inside a mapped folder, e.g. /config/...).
  log_level:
    name: Log level
    description: MQTT-IO logging verbosity.